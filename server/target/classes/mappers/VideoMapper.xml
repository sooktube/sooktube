<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 
<mapper namespace="com.SOOKTUBE.dao.VideoDAO">
 
    <insert id="newVideo"
            parameterType="com.SOOKTUBE.model.VideoDTO">
        INSERT INTO VIDEO(`videoID`, `videoTitle`, `videoDesc`, `videoDate`, `userID`)
        VALUES (`videoID`, #{videoTitle}, #{videoDesc}, #{videoDate}, #{userID})
    </insert>
 
</mapper>


<!-- videoID int(11) PK 
videoTitle varchar(20) 
videoDesc text 
videoDate date 
userID varchar(50) -->